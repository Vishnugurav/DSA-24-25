#include<stdio.h>
#include<stdlib.h>
#include "header.h"

int main(){

    //Intialize the polynomoials
    poly poly1;
    init (&poly1,5);
    poly poly2;
    init (&poly2, 3);

    //create polynomials
    printf("Enter the details for polynomial");
    create(&poly1);

}